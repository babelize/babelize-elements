---
"@babelize/elements": minor
---

Add shadcn-compatible component registry (`/r/[name].json` + `/registry.json`) and a bundled CLI (`npx @babelize/elements add <component>` / `list`). Components are now styled with plain Tailwind classes instead of CSS-variable design tokens, and the theming docs/theme-switcher demos have been removed.