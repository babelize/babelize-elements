---
title: Language Switcher Pill
description: Animated pill-style language switcher for React with keyboard navigation and RTL support.
---

import { PreviewComponents } from "@/components/docs/preview-components";
import { LanguageSwitcherPillDemo } from "@/components/docs/language-switcher-pill-demo";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

<PreviewComponents registryName="language-switcher-pill">
  <LanguageSwitcherPillDemo />
</PreviewComponents>

## Installation

<Tabs
  items={["shadcn", "Elements CLI"]}
  defaultValue="shadcn"
  className="bg-transparent border-none"
>
  <Tab value="shadcn" className="border-none bg-transparent p-0 mt-3">
    <Tabs items={["npx", "pnpm", "yarn", "bun"]} defaultValue="npx" groupId="installation-tabs">
      <Tab value="npx">```bash npx shadcn@latest add @elements/language-switcher-pill ```</Tab>
      <Tab value="pnpm">
        ```bash pnpm dlx shadcn@latest add @elements/language-switcher-pill ```
      </Tab>
      <Tab value="yarn">
        ```bash yarn dlx shadcn@latest add @elements/language-switcher-pill ```
      </Tab>
      <Tab value="bun">```bash bunx shadcn@latest add @elements/language-switcher-pill ```</Tab>
    </Tabs>
  </Tab>
  <Tab value="Elements CLI" className="border-none bg-transparent p-0 mt-3">
    <Tabs items={["npx", "pnpm", "yarn", "bun"]} defaultValue="npx" groupId="elements-cli-tabs">
      <Tab value="npx">```bash npx @babelize/elements add language-switcher-pill ```</Tab>
      <Tab value="pnpm">```bash pnpm dlx @babelize/elements add language-switcher-pill ```</Tab>
      <Tab value="yarn">```bash yarn dlx @babelize/elements add language-switcher-pill ```</Tab>
      <Tab value="bun">```bash bunx @babelize/elements add language-switcher-pill ```</Tab>
    </Tabs>
  </Tab>
</Tabs>

## Usage

```tsx
import { LanguageSwitcherPill } from "@/components/ui/language-switcher-pill";
```
